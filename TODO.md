# Scroll to Top Functionality TODO

## Plan Steps:
- [x] Step 1: Create `src/components/ScrollToTop.tsx` component using `useLocation` and `window.scrollTo`.
- [x] Step 2: Update `src/App.tsx` to import and render `<ScrollToTop />` in the store layout (inside `min-h-screen flex flex-col` div, after `<Navbar />`).
- [x] Step 3: Test navigation between pages (Home → Products → ProductDetail → etc.) to confirm scroll-to-top on each change.
- [x] Step 4: Mark complete and run `attempt_completion`.

**Status**: 4/4 completed. ✅

